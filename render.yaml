services:
  - type: web
    name: epbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python artifacts/elited-vip/main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: PORT
        value: 10000
